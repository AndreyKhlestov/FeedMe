# from aiogram.filters.callback_data import CallbackData
#
#
# class ReferralUrlCallback(CallbackData, prefix='referral_url'):
#     id: int  # id реферальной ссылки из БД
#
#
# class DealDataCallback(CallbackData, prefix='deal_data'):
#     id: int  # id сделки
#     after: int  # с какой сделки начать показывать данные (для пагинации - получаю из запроса к api)
#
#
# class PaginationDealsProcessing(CallbackData, prefix='deals_in_processing'):
#     after: int  # с какой сделки начать показывать данные (для пагинации - получаю из запроса к api)

